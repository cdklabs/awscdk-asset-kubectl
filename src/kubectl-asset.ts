import * as path from 'path';

export const ASSET_FILE = path.join(__dirname, 'layer.zip');
export const LAYER_SOURCE = path.join(__dirname, '..', 'layer', 'Dockerfile');
export const LAYER_SOURCE_DIR = path.join(__dirname, '..', 'layer');
